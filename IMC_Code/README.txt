The scripts are based on python 3, running in Linux as command line scrips.

The user will need to comment out the read dss portion of scripts, and add code to read data from excel or csv files instead, the remaining part of scripts will be same. 

Stage Envelope spreadsheet provided, script is not stand-alone.
